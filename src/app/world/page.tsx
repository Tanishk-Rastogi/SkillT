import { WorldMap } from '@/components/world/WorldMap';

export const metadata = {
  title: 'World | SkillTree',
  description: 'Navigate the living regions of the SkillTree civilization.',
};

export default function WorldPage() {
  return <WorldMap />;
}
